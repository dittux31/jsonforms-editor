{
  "name": "root",
  "scripts": {
    "lerna": "lerna",
    "initialStart": "npm ci && npm run init && npm run build && npm run start",
    "init": "lerna bootstrap --hoist",
    "build": "lerna run build",
    "test": "lerna run test",
    "lint": "lerna run lint",
    "start": "cd app && npm run start",
    "start::v18": "cd app && npm run start::v18",
    "watch": "cd jsonforms-editor && npm run watch",
    "cypress:open": "cd app && npm run cypress:open",
    "cypress:ci": "cd app && npm run cypress:ci"
  },
  "devDependencies": {
    "jest-environment-jsdom-fourteen": "^1.0.1",
    "lerna": "^4.0.0"
  }
}
